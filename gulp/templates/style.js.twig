class StyleTemplate_{{ name }} {
	/**
	 * Injects style into DOM
	 */
	inject (callback) {
	};

	/**
	 * Reloads injected style in DOM (busts cache, causes partially browser rendering)
	 */
	reload (callback) {
	};

	/**
	 * Removes the stylesheet from DOM (when injecting again browser cache won't be busted)
     */
	remove (callback) {
	};
};

export default instance = new TwigTemplate_{{ name }}();